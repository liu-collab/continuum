# memory-native-agent e2e

这里保留 `T10` 的端到端验收资产入口。

当前阶段先完成：

- 人工验收清单
- 已有 HTTP / WS / runner / session-store 测试的收口说明

还没有正式补齐：

- 真正的跨服务 e2e 测试进程编排
- Playwright UI e2e
- record/replay provider 驱动的端到端 fixture

这部分在下一轮继续补真实用例时，建议按 `docs/memory-native-agent/T10-e2e-acceptance.md` 的场景清单继续展开。
